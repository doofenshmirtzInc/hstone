// this is a c++ file
// .cc is an alternate extension for .cpp


#include <iostream>
#include <cstdlib>

using namespace std;


int
main(){

	// game loop
	while(True){
		// game loop steps ( according to game loop tut )
		// while(userNotQuitOrCrash)
		// 1. updatePhysics()
		// 2. Scene.updateScenePicture()
		// 3. passToNOdeScripts(userInput) -> update according to user input
		// 4. performanceUPdate()
		// 5. userNOtQuitOrCrash = updateAppState()
	}

	return 0;
}
